# numpy >= 1.7.1
# scipy >= 0.12.0
# matplotlib >= 1.2.1

file:../lib/pywikipedia/#egg=pywikipedia
# Temporary. We depend on these only for
# setup files; this will go away with buildout.
# They actually depends on us but doesn't declare that
-e svn+https://repos.nextthought.com/svn/nti-svn/nti.app.client_preferences/trunk#egg=nti.app.client_preferences
-e svn+https://repos.nextthought.com/svn/nti-svn/nti.app.sites.mathcounts/trunk#egg=nti.app.sites.mathcounts

# Also temporary, going away with buildout
-e svn+https://repos.nextthought.com/svn/nti-svn/nti.testing/trunk#egg=nti.testing
-e svn+https://repos.nextthought.com/svn/nti-svn/nti.app.testing/trunk#egg=nti.app.testing


## Sadly, issue 328 prevents us from using local paths for these things,
## if we include the egg name. If we're otherwise very careful,
## local paths can be made to work
## see https://github.com/pypa/pip/issues/328

#-e ../site-lib/geventwebsocket
